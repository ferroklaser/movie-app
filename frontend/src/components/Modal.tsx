const Modal = (
    { isVisible,
        children
    }: {
        isVisible: boolean,
        children: React.ReactNode
    },
) => {
    return (
        <div className="flex-1 bg-green-500">

        </div>
    )
}

export default Modal